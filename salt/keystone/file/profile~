# ~/.profile: executed by Bourne-compatible login shells.
export OS_USERNAME=admin
export OS_PASSWORD=1
export OS_TENANT_NAME=admin
export OS_AUTH_URL=http://10.10.10.81:35357/v2.01

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

mesg n
